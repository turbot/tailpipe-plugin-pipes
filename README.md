# tailpipe-plugin-pipes

Tailpipe plugin to collect and query Turbot Pipes audit log data.

## Developers

Build a developer version of the plugin:
```
make
```

Check it was created and installed:
```
ls -al ~/.tailpipe/plugins
```
